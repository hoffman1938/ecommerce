# ==========================================================================
# Host-machine development template (apps run with pnpm on the host,
# infrastructure services run in Docker via `docker compose up postgres
# redis minio minio-init mailpit`).
#
# Copy to `.env.local` (and to `apps/*/.env.local` for the Next.js apps).
# ==========================================================================

NODE_ENV=development

STOREFRONT_URL=http://localhost:3000
ADMIN_URL=http://localhost:3001
API_URL=http://localhost:4000

NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_STOREFRONT_URL=http://localhost:3000
NEXT_PUBLIC_ADMIN_URL=http://localhost:3001
NEXT_PUBLIC_ASSET_BASE_URL=http://localhost:9000

DATABASE_URL=postgresql://outlet:outlet@localhost:5432/outlet
REDIS_URL=redis://localhost:6379

STORAGE_PROVIDER=minio
S3_ENDPOINT=http://localhost:9000
S3_PUBLIC_ENDPOINT=http://localhost:9000
S3_BUCKET=outlet-local
S3_REGION=us-east-1
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
S3_FORCE_PATH_STYLE=true

EMAIL_PROVIDER=smtp
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM="Outlet Marketplace <no-reply@outlet.local>"

PAYMENT_PROVIDER=mock
MOCK_PAYMENT_WEBHOOK_SECRET=local-mock-webhook-secret
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Optional real Super Admin created by `pnpm db:seed`. Keep the real values in
# `.env.local` (gitignored), never here. Re-seeding rotates the password.
SEED_SUPERADMIN_EMAIL=
SEED_SUPERADMIN_PASSWORD=
SEED_SUPERADMIN_FIRST_NAME=
SEED_SUPERADMIN_LAST_NAME=

SESSION_SECRET=local-dev-session-secret-change-me
SESSION_COOKIE_NAME=outlet_session
CART_COOKIE_NAME=outlet_cart
COOKIE_DOMAIN=
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
TRUSTED_ORIGINS=http://localhost:3000,http://localhost:3001
CAPTCHA_PROVIDER=none
TURNSTILE_SECRET_KEY=

RESERVATION_DURATION_MINUTES=20
DEFAULT_CURRENCY=EUR
DEFAULT_LOCALE=en

RESERVATION_SWEEP_INTERVAL_SECONDS=30
CAMPAIGN_SWEEP_INTERVAL_SECONDS=60

API_PORT=4000
API_HOST=0.0.0.0
TRUST_PROXY=false

RATE_LIMIT_WINDOW_SECONDS=60
RATE_LIMIT_MAX_REQUESTS=300
AUTH_RATE_LIMIT_MAX_REQUESTS=10
